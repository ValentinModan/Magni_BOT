package board.moves.movetypes;

import board.Position;

public class CastleMove extends Move
{
    public CastleMove(Position initialPosition, Position finalPosition)
    {
        super(initialPosition, finalPosition);
    }
}
